@using _71BootlegStore.ViewModels
@model ProductManagementVM
@{
    ViewData["Title"] = "Product Management";
}

<div class="content">

    <h2 class="intro-y text-lg font-medium mt-10">
        Create Product
    </h2>

    <div class="intro-y mt-3">

        <form id="frmAuthUser" method="post" enctype="multipart/form-data">
            @* User Form *@
            <div class="intro-y box p-5 mt-5">
                <div class="border border-slate-200/60 rounded-md p-5">
                    <div class="font-medium text-base flex items-center border-b border-slate-200/60 pb-2">@Model.Title</div>

                    <div class="text-danger" asp-validation-summary="All"></div>
                    <input type="hidden" asp-for="@Model.Title">

                    <div class="col-span-12 mt-3">
                        <div class="form-inline grid grid-cols-12 gap-2">
                            <label asp-for="@Model.ProductManagement.Name"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Name)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Name" required>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Name"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Image"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Image)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input name="file" type="file" class="form-control" asp-for="@Model.ProductManagement.Image">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Image" hidden>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Image"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Quantity"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Quantity)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Quantity" required>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Quantity"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Gender"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Gender)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <select class="form-select" asp-for="@Model.ProductManagement.Gender" required>
                                    <option value="true">ผู้ชาย</option>
                                    <option value="false">ผู้หญิง</option>
                                </select>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Gender"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Price"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Price)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Price" required>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Price"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Color"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Color)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Color" required>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Color"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Size"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Size)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.ProductManagement.Size" required>
                                <span class="text-danger" asp-validation-for="@Model.ProductManagement.Size"></span>
                            </div>

                            <label asp-for="@Model.ProductManagement.Status"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.ProductManagement.Status)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <select class="form-select" asp-for="@Model.ProductManagement.Status" required>
                                    <option value="true" selected>Active</option>
                                    <option value="false">Inactive</option>
                                </select>
                                <span class="text-danger"></span>
                            </div>

                        </div>
                    </div>

                </div>
            </div>

            <input type="hidden">

            <div class="flex justify-center flex-col md:flex-row gap-2 mt-5">
                <a asp-controller="ProductManagement" asp-action="Index" class="btn border-slate-300 dark:border-darkmode-400 text-slate-500 w-full md:w-52">Cancel</a>
                <button type="submit" class="btn btn-primary w-full md:w-52">Save</button>
            </div>

        </form>

    </div>
</div>

@section Scripts
{
    <script>
        const breadcrumbs = [
            {
                "label": "Master Data",
                "url": "javascript:;"
            },
            {
                "label": "Authorize",
                "url": "javascript:;"
            },
            {
                "label": "Create",
                "url": "javascript:;"
            }
        ];
        activeMenu(breadcrumbs);

        $(document).ready(function () {
           
        });
    </script>
}