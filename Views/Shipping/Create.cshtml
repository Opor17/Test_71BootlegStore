@using _71BootlegStore.ViewModels
@model ShippingVM
@{
    ViewData["Title"] = "Shipping";
}

<div class="content">

    <h2 class="intro-y text-lg font-medium mt-10">
        Create Shipping
    </h2>

    <div class="intro-y mt-3">

        <form id="frmAuthUser" method="post">
            @* User Form *@
            <div class="intro-y box p-5 mt-5">
                <div class="border border-slate-200/60 rounded-md p-5">
                    <div class="font-medium text-base flex items-center border-b border-slate-200/60 pb-2">@Model.Title</div>

                    <div class="text-danger" asp-validation-summary="All"></div>
                    <input type="hidden" asp-for="@Model.Title">

                    <div class="col-span-12 mt-3">
                        <div class="form-inline grid grid-cols-12 gap-2">

                            <label asp-for="@Model.Shipping.Name"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.Shipping.Name)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.Shipping.Name" required>
                                <span class="text-danger" asp-validation-for="@Model.Shipping.Name"></span>
                            </div>

                            <label asp-for="@Model.Shipping.ShippingPrice"
                                   class="form-label col-span-12 md:col-span-2 xl:col-span-2 2xl:col-span-2 mr-0">
                                <span class="text-danger mr-1">*</span>@Html.LabelFor(m => m.Shipping.ShippingPrice)
                            </label>
                            <div class="col-span-12 md:col-span-4 xl:col-span-4 2xl:col-span-4">
                                <input type="text" class="form-control" asp-for="@Model.Shipping.ShippingPrice" required>
                                <span class="text-danger" asp-validation-for="@Model.Shipping.ShippingPrice"></span>
                            </div>

                        </div>
                    </div>

                </div>
            </div>

            <input type="hidden">

            <div class="flex justify-center flex-col md:flex-row gap-2 mt-5">
                <a asp-controller="Shipping" asp-action="Index" class="btn border-slate-300 dark:border-darkmode-400 text-slate-500 w-full md:w-52">Cancel</a>
                <button type="submit" class="btn btn-primary w-full md:w-52">Save</button>
            </div>

        </form>

    </div>
</div>

@section Scripts
{
    <script>
        const breadcrumbs = [
            {
                "label": "Master Data",
                "url": "javascript:;"
            },
            {
                "label": "Shipping",
                "url": "javascript:;"
            }
        ];
        activeMenu(breadcrumbs);

        $(document).ready(function () {

        });
    </script>
}